# Stage 2 — voice (install on top of requirements.txt)
# System deps first:  sudo apt install portaudio19-dev
faster-whisper>=1.0
piper-tts>=1.2
sounddevice>=0.4
numpy>=1.24

# Stage 4 — hands-free (wake word + voice-activity detection)
openwakeword>=0.6
onnxruntime>=1.16
webrtcvad>=2.0
