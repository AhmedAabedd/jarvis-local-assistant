# Knowledge index — the menu of what you know

# This menu is all you see by default; files aren't loaded automatically.
# When a task needs one, open it with read_file(path) and use what's inside.
# Paths are relative to: ~/mounir_assistant/jarvis-local-assistant/knowledge/

- contacts.md — Ahmed's address book (name/alias -> email). READ before emailing a person by name, to get their address; stop and ask the user if the name isn't listed.
- email_style.md — Ahmed's email tone + ready-made templates. READ when writing an email body, so it sounds like Ahmed.

# Added a new knowledge file? Add one line here too, or you'll forget it exists.
