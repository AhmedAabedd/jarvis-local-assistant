# Stage 1 — core text assistant
ollama>=0.3
langgraph>=0.2

# Stage 3 — tools
ddgs>=6.0
# fetch_url: download a page and extract its readable text
requests>=2.28
beautifulsoup4>=4.11

mistralai>=1.0
# Groq: supervisor streaming (USE_GROQ) + the llm.groq_chat helper
groq>=0.13

# CLI — live markdown rendering of the streamed reply
rich>=13.0

# Telegram bridge (telegram_cli.py) — long-polling bot
pyTelegramBotAPI>=4.14

# play_on_youtube: resolve a search to the top video, no API key
yt-dlp>=2024.1.1

# Email specialist — MCP client SDK; the Gmail MCP server itself runs via npx
mcp>=1.0