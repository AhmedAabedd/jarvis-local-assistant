# Core agent runtime
# Graph primitives: MessagesState, ToolNode, Command handoffs, and streaming.
langgraph>=1.1,<2
langchain-core>=1.0,<2

# HTTP integrations
requests>=2.28

# CLI — live markdown rendering of the streamed reply
rich>=13.0

# Telegram bridge (started by server.py; telegram_cli.py remains standalone)
pyTelegramBotAPI>=4.14

# play_on_youtube: resolve a search to the top video, no API key
yt-dlp>=2024.1.1

# MCP clients. Streamable HTTP support uses the current SDK client API.
mcp>=1.27,<2
httpx>=0.27
PyYAML>=6.0

# File and media specialist — format handlers are lazy-imported so an
# unavailable codec produces a focused tool error instead of breaking startup.
Pillow>=10.0
pypdf>=4.0
PyMuPDF>=1.24
openpyxl>=3.1
python-docx>=1.1
python-pptx>=1.0
reportlab>=4.0
opencv-python-headless>=4.9

# Web dashboard + admin API
fastapi>=0.110
uvicorn>=0.29
psutil>=5.9
python-multipart>=0.0.9
